message = "Hello World"print(message)message = "Hello python Crash Course World!"print(message)#rules# Variable names can only contain letters, numbers and underscores#spaces are not allowed in variable names but _ can be used to seperate# avoid using python keywords and function anmes as variable names# Variable names should be short but descriptive# be carefule with using l and uppercase O as they can get confused with 1 and 0mesage = "Error Test"print(mesage)